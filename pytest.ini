[pytest]
testpaths = tests
addopts = -m "not slow"
markers =
    slow: marks tests as slow (deselected by default; run with `pytest -m slow` to include)
filterwarnings =
    ignore::DeprecationWarning
    ignore::UserWarning
